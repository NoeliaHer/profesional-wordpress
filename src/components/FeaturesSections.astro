---
const { align = 'center', title, subtitle, items, icons, backgroundImage } = Astro.props;

interface Item {
  title: string;
  content: string;
}

interface Icon {
  name: string;
}
---


  <div class="relative mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:text-center relative z-10">
      <h2 class="text-base font-semibold leading-7 text-indigo-600">{subtitle}</h2>
      <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">{title}</p>
    </div>
    <div class="relative mx-auto mt-16 max-w-2xl sm:mt-20 lg:mt-24 lg:max-w-7xl z-10">
      <dl class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 lg:max-w-none lg:gap-y-16">
        {items.map((item: Item, index: number) => (
          <div class="feature-box bg-white text-black p-6 rounded-lg shadow-md relative pl-16 h-60" key={index}>
            <div class="absolute left-0 top-0 flex h-10 w-10 items-center justify-center rounded-lg bg-gray-200">
              <i class={`h-6 w-6 text-black ${icons[index]}`}></i>
            </div>
            <div class="text-center">
              <dt class="text-2xl font-semibold leading-7 text-black font-bold mt-4">
                {item.title}
              </dt>
              <dd class="mt-2 text-base leading-7 text-gray-700">
                {item.content}
              </dd>
            </div>
          </div>
        ))}
      </dl>
    </div>
  </div>
</div>

<style>
  .feature-box {
    background-color: white;
    color: black;
    padding: 1rem;
    border-radius: 0.5rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    position: relative;
    height: auto;
  }

  .feature-box dt {
    color: black;
    font-weight: bold;
    font-size: 1.5rem;
  }

  .feature-box .text-center {
    text-align: center;
  }
</style>
