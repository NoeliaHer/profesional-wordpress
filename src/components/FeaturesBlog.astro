---
export interface Feature {
  image: string;
  title: string;
  description: string;
  link?: string;
}

export interface Props {
  features: Feature[];
}

const { features } = Astro.props;
---

<div class="bg-white px-2 py-10 w-full">
  <div id="features" class="container mx-auto">
    <p class="title">Importancia del Mantenimiento Web</p>
    <h2 class="subtitle">El mantenimiento web no es una tarea única, sino un proceso continuo que impulsa varios beneficios:</h2>
    <ul class="mt-16 flex flex-wrap justify-center gap-6 text-center text-slate-700">
      {features.map(feature => (
        <li class="rounded-xl bg-white px-6 py-8 shadow-sm flex-1 max-w-xs">
          {feature.link ? (
            <a href={feature.link} class="group">
              <img src={feature.image} alt={feature.title} class="mx-auto h-10 w-10 icon-blue">
              <h3 class="my-3 font-display font-medium group-hover:text-primary-500 text-blue-500">{feature.title}</h3>
              <p class="mt-1.5 text-sm leading-6 text-secondary-500">{feature.description}</p>
            </a>
          ) : (
            <>
              <img src={feature.image} alt={feature.title} class="mx-auto h-10 w-10 icon-blue">
              <h3 class="my-3 font-display font-medium text-blue-500">{feature.title}</h3>
              <p class="mt-1.5 text-sm leading-6 text-secondary-500">{feature.description}</p>
            </>
          )}
        </li>
      ))}
    </ul>
  </div>
</div>

<style>
  .title {
    font-size: 2rem;
    color: #003366; /* Dark blue color */
    text-align: left; /* Align text to the left */
  }

  .subtitle {
    font-size: 1.25rem;
    color: rgba(55, 65, 81, var(--tw-text-opacity)); /* Secondary color */
    margin-bottom: 2rem;
    line-height: 1.625; /* Match the line height of the text next to the image */
    text-align: left; /* Align text to the left */
  }

  .container {
    width: 100%;
    max-width: 100%;
    padding-left: 1rem;
    padding-right: 1rem;
  }

  .rounded-xl {
    border-radius: 1rem;
  }

  .shadow-sm {
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  }

  .group:hover .group-hover\:text-primary-500 {
    color: var(--primary-color, #0000ff);
  }

  .text-primary-500 {
    color: var(--primary-color, #0000ff);
  }

  .text-secondary-500 {
    color: var(--secondary-color, #6b7280);
  }

  .text-blue-500 {
    color: #07079e; /* Blue color for titles */
  }

  .icon-blue {
    filter: invert(22%) sepia(98%) saturate(1829%) hue-rotate(187deg) brightness(96%) contrast(101%);
    /* This filter changes the icon color to blue */
  }

  @media (min-width: 768px) {
    .flex-1 {
      flex: 1 1 0%;
    }
  }

  .max-w-xs {
    max-width: 20rem; /* Adjust as needed */
  }
</style>
